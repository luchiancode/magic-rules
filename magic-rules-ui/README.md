# Magic Rules UI

## Setup

1. Ensure you have Node.js and `pnpm` installed.
2. Install the project dependencies:
   ```bash
   pnpm install
   ```

## Run

To start the Angular UI app:
```bash
pnpm nx serve
```
